import { ActindexContentPipe } from './act.pipe';

describe('ActindexContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ActindexContentPipe();
    expect(pipe).toBeTruthy();
  });
});
